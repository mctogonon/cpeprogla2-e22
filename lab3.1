#include<iostream>
using namespace std;

main(){
	int x=11, y=6, z=22, *p1, *p2;
	p1 = &z;
	p2 = &x;
	
	cout << "x:" << x << " y:" << y << " z:" << z << " *p1:" << *p1 << " *p2:" << *p2 << endl;
	
	p1 = p2;
	
	cout << "x:" << x << " y:" << y << " z:" << z << " *p1:" << *p1 << " *p2:" << *p2 << endl;
	
	p1 = &z;
	
	cout << "x:" << x << " y:" << y << " z:" << z << " *p1:" << *p1 << " *p2:" << *p2 << endl; 
	
	z=11;
	
	cout << "x:" << x << " y:" << y << " z:" << z << " *p1:" << *p1 << " *p2:" << *p2 << endl;
	
	z =13;
	
	cout << "x:" << x << " y:" << y << " z:" << z << " *p1:" << *p1 << " *p2:" << *p2 << endl;
	
	z = y;
	
	cout << "x:" << x << " y:" << y << " z:" << z << " *p1:" << *p1 << " *p2:" << *p2 << endl;
}
